app = 'fly-io-wild-fire-1232'
primary_region = 'nrt'

[build]
  dockerfile = 'Dockerfile'

[env]
  PORT = '80'

[[services]]
  internal_port = 80
  protocol = 'udp'

  [[services.ports]]
    port = 80